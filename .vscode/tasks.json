{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "server-build",
      "command": "yarn build",
      "options": {
        "cwd": "${workspaceFolder}/server"
      }
    }
  ]
}
